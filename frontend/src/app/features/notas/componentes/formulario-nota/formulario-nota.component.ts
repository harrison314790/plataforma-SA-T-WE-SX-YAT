import { Component } from '@angular/core';

/**
 * TODO: formulario de una nota individual (usado en la celda editable de
 * tabla-notas o como panel aparte en móvil). Ver references/angular.md.
 */
@Component({
  selector: 'app-formulario-nota',
  standalone: true,
  templateUrl: './formulario-nota.component.html',
})
export class FormularioNotaComponent {}
