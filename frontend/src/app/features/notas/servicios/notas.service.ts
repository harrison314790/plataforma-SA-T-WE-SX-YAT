import { Injectable } from '@angular/core';

/**
 * TODO: implementar contra POST/GET /api/notas (ver
 * references/node-supabase.md para el contrato del backend y
 * references/angular.md para el patrón de guardado optimista con
 * el punto de "sin guardar").
 */
@Injectable({ providedIn: 'root' })
export class NotasService {}
